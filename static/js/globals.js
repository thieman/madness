var width = 1300,
	height = 900;

var svg = d3.select('body').append('svg')
    .attr('width', width)
    .attr('height', height);

var userImageSize = 100,
    userGridSpacing = 20,
    userGridRowSize = 9;
